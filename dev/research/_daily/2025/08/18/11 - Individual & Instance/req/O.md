# 1.
`O` ≔ (https://en.wikipedia.org/wiki/Ontology_(information_science))

# 2.
## 2.1.
`Ls` ≔ ⠿~ (https://en.wikipedia.org/wiki/Ontology_language)

## 2.2.
`Li` : `Ls`

# 3.
## 3.1.
`T1` ≔ (Термин «invididual» в `Ls`)

## 3.2.
`T2` ≔ (Термин «instance» в `Ls`)

## 3.3.
`C1` ≔ (Термин «concept» в `Ls`)

## 3.3.
`C2` ≔ (Термин «class» в `Ls`)

# 4.
## 4.1.
`Ts` ≔ ⠿{`T1`, `T2`}

## 4.2.
`Ti` : `Ts`

# 5.
## 5.1.
`Cs` ≔ ⠿{`C1`, `C2`}

## 5.2.
`Ci` : `Cs`

# 6.
## 6.1.
`L-C1s` ≔ ⠿~ (`Li`, которые предпочитают использовать `C1` вместо `C2`)

## 6.2.
`L-C2s` ≔ ⠿~ (`Li`, которые предпочитают использовать `C2` вместо `C1`)

## 6.3.
`L-T1s` ≔ ⠿~ (`Li`, которые предпочитают использовать `T1` вместо `T2`)

## 6.4.
`L-T2s` ≔ ⠿~ (`Li`, которые предпочитают использовать `T2` вместо `T1`)

# 7.
## 7.1.
`L-C1-T1s` ≔ `L-C1s` ∩ `L-T1s`

## 7.2.
`L-C1-T2s` ≔ `L-C1s` ∩ `L-T2s`


