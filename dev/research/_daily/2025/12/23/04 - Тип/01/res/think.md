https://gemini.google.com/share/810ca547088e

Ниже представлен анализ терминологии для обозначения сущности `T` в 20 популярных языках описания онтологий и моделирования (`OL`).

## 1. Стандарты Semantic Web и Linked Data

В спецификации Web Ontology Language (OWL) сущность `T` официально именуется термином `Class`.
В стандарте RDF Schema (RDFS) для обозначения `T` используется термин `Class`.
В системе организации знаний SKOS `T` носит название `Concept`.
В словаре семантической разметки Schema.org категории определяются термином `Type`.
В формате биомедицинских онтологий OBO для `T` используется ключевое слово `Term`.
В языке разметки DAML+OIL сущность `T` называется `Class`.
В семействе языков WSML для описания элементов домена применяется термин `Concept`.

## 2. Системы представления знаний и логические языки

В языке базы знаний Cyc (CycL) `T` называется уникальным термином `Collection`.
В логическом языке F-Logic для группировки объектов используется термин `Class`.
В классической системе KL-ONE родовые сущности описываются термином `Concept`.
В языке представления знаний LOOM `T` именуется `Concept`.
В системе Ontolingua для обозначения классов используется термин `Class`.
В языке операционного моделирования OCML `T` называется `Class`.
В инфраструктуре KAON для `T` принят термин `Concept`.
В языке NKRL для описания понятий используется термин `Concept`.

## 3. Языки моделирования и инженерии

В унифицированном языке моделирования UML абстракция `T` носит название `Class`.
В модели «сущность-связь» (ER) для классификации объектов используется термин `Entity Type`.
В стандарте ISO Topic Maps сущность `T` определяется как `Type`.
В методологии IDEF5 для естественно возникающих видов принят термин `Kind`.
В формальном языке Gellish классы вещей обозначаются термином `Kind`.