# 1. ≔
## Syntax
~~~code
A ≔ B
~~~
## Meaning
`B`: некое понятие.
`A`: краткое обозначение для `B`.  
Таким образом, `A ≔ B` means that `A` denotes `B`.
## Example
### Code
`DOR` ≔ Florida Department of Revenue
### Explanation
`DOR` — краткое обозначение для «Florida Department of Revenue».

# 2. ⟨ … ⟩
## Syntax
~~~code
⟨ A ⟩
~~~
## Meaning
`⟨ A ⟩` ≡ `A`
## Rationale
Я использую `⟨ A ⟩` вместо `A` с целью яснее показать границы `A`.  
Обычно я делаю это в следующих 3 cases:
### Case 1
Когда `A` — длинный текст.
### Case 2
Когда `A` внутри себя содержит спецсимволы.
#### Example
##### Code
`M `≔ ⟨ Marketplace facilitators (Airbnb/VRBO) ⟩
##### Explanation
Обычные скобки `( )` уже являются частью значения, и нестандартными скобками `⟨ ⟩` я яснее показываю границы значения.
### Case 3
Когда `A` — многострочный текст.  
В этом случае я могу использовать `⟨ ⟩` для сокращения количества строк, потому что `⟨ ⟩`, в отличие от стандартного синтаксиса Markdown для обозначения границ блоков посредством backticks/tildas, позволяет мне размещать первую и последнюю строки значения на тех же строках, что и маркеры границ: `⟨ ⟩`.
#### Example
~~~example
⟨ Line 1
Line 2
<…>
Last Line ⟩
~~~
##### Explanation
Такая запись эквивалентна стандартному синтаксису Markdown (но при этом короче):
###### Backticks
~~~~example
```markdown
Line 1
Line 2
<…>
```
~~~~
###### Tildas
~~~~example
~~~markdown
Line 1
Line 2
<…>
~~~
~~~~ 

# 3. Ⱳ
## Syntax
~~~code
Ⱳ URL
~~~
## Meaning
Понятие, которому посвящена статья Wikipedia по адресу `URL`.
## Example
`Ⱳ https://en.wikipedia.org/wiki/Ontology_(information_science)` ≡ ⟨ An ontology: a representation, formal naming, and definitions of the categories, properties, and relations between the concepts, data, or entities. ⟩

# 4. ⟪ … ⟫
## Syntax
~~~code
⟪ M ⟫ A
~~~

## Meaning
`M` — ⟨ `Ⱳ https://en.wikipedia.org/wiki/Metadata` ⟩ about `A`.

## Example
~~~example
⟪ мой ответ клиенту на его письмо `X` ⟫
```markdown
<содержание моего ответа>
```
~~~
#### Explanation
- `<содержание моего ответа>` — это текст моего письма клиенту.
- «мой ответ клиенту на его письмо `X`» — metadata. 
- Metadata не является частью текста моего письма клиенту: она указывает назначение текста. 

# 5. ⊤
## Syntax
~~~code
⊤ A
~~~
## Meaning
`A` is an ⟨ `Ⱳ https://en.wikipedia.org/wiki/Axiom` ⟩.
## Rationale
Я обычно использую синтаксис `⊤` на поздних этапах многошагового анализа.  
На ранних этапах такого анализа истинность `A` может быть неочевидна.   
Истинность `A` может выясниться в процессе анализа.   
В таком случае я явно пишу `⊤ A`, чтобы ты не тратил свои ресурсы на повторный анализ истинности `A` и в дальнейшем анализе считал `A` аксиомой.

# 7. ?
## Syntax
~~~code
? A
~~~
## Meaning
`A` is a hypothesis.

# 8.
##
`𝒞` ≔ ⟨ понятие «category» в контексте ⟨ Ⱳ https://en.wikipedia.org/wiki/Theory_of_categories ⟩ ⟩ 

##
`ℂ` ≔ 
```markdown
понятие «class» в контекстах:
- ⟨ Ⱳ https://en.wikipedia.org/wiki/Class_(knowledge_representation) ⟩ 
- ⟨ Ⱳ https://en.wikipedia.org/wiki/Ontology_components#Classes ⟩
``` 

##
`𝕀` ≔ ❮ понятие «interval» в контексте 
⟨ Ⱳ https://en.wikipedia.org/wiki/Partially_ordered_set#Intervals ⟩ ❯ 

##
`ℙ` ≔ 
```markdown
понятие «predicate» в контекстах:
- ⟨ Ⱳ https://en.wikipedia.org/wiki/Predicate_(logic) ⟩ 
- ⟨ Ⱳ https://en.wikipedia.org/wiki/First-order_logic ⟩
- ⟨ Ⱳ https://en.wikipedia.org/wiki/Function_symbol ⟩
``` 

##
`𝕊` ≔ ⟨ Ⱳ https://en.wikipedia.org/wiki/Set_(mathematics) ⟩

##
`𝕋` ≔ ⟨ понятие «type» в контексте ⟨ Ⱳ https://en.wikipedia.org/wiki/Type_theory ⟩ ⟩

# 9. ⠿{I₁, I₂, …, Iₙ} 
## Meaning
Такая запись определяет `𝕊` посредством точного (полного) перечисления его элементов. 
## Example
~~~code
⠿{1, 2, 5, 7}
~~~

# 10. ┫…┣
## Syntax
~~~code
⟪ M ⟫ ┫A - B┣
~~~
## Meaning
###
Такая запись определяет `𝕀` (упорядоченное `𝕊`) посредством указания его границ `A` и `B` среди некоего упорядоченного надмножества `M`.
Итоговый `𝕊` / `𝕀` включает `A` и `B`.
## Examples
###
`⟪ пункты документа X ⟫ ┫1.2 - 5┣` означает «`𝕊` из пунктов документа `X` от пункта 1.2 до пункта 5 включительно».
###
`⠿ ⟪ месяцы ⟫ ┫январь - март┣` означает «⟪ месяцы ⟫ `⠿{январь, февраль, март}`».

# 11. ⠿ ⟨ ℙ ⟩
## Syntax
~~~code
⠿ ⟨ ℙ ⟩
~~~
## Meaning
Такая запись определяет `𝕊` / `𝕋` посредством `ℙ`: формальной спецификации, которой должны соответствовать элементы этого `𝕊` / `𝕋`. 
## Examples
~~~code
⠿ ⟨ натуральные числа ⟩
~~~ 

# 11. ⠿~
## Syntax
~~~code
⠿~ D 
```examples  
- Example 1
- Example 2
- Example 3
```
~~~
## Meaning
Такая запись определяет `𝕊` / `𝒞` / `ℂ` посредством неточного, примерного словесного описания `D`.
`<examples>` — опциональный блок, перечисляющая некоторые элементы в качестве примеров.
###
`D` может описывать элементы как во множественном числе, так и в единственном. 
Оба этих варианта записи эквивалентны друг другу: `⠿~ ⟨ balls ⟩` ≡ `⠿~ ⟨ ball ⟩`.
## Example
~~~code
⠿~ ⟨ a social media platform where users can express their opinions independently of the platform owner ⟩
```examples
- `Reddit`
- GitHub
- Сайты сети StackExchange
- Авторитетные сайты отзывов типа Trustpilot, sikayetvar.com и т.п.
```
~~~
Здесь `𝕊` / `𝒞` / `ℂ` «social media platforms» определено неточно, примерно. 

# 12. ᗉ
## Syntax
~~~code
ᗉ S
~~~
## Meaning
```text
Любой / каждый элемент `S`
```
При этом `S` — это `𝕊` / `𝒞` / `ℂ` / `𝕋`.
## Rationale
Я использую такой синтаксис, чтобы удобным образом делать утверждения, которые истинны для каждого элемента `S`.
## Example
`A ≔ ᗉ ⠿ ⟨ моё письмо клиенту ⟩` означает, что `A` — это не какое-либо конкретное моё письмо клиенту, а обозначение для любого / каждого моего письма клиентам.
Дальше я могу удобным мне образом формулировать требования сразу ко всем моим письмам клиентам, Например: 
```markdown
Каждый абзац `A` должен содержать ровно одно предложение.
Между абзацами `A` не должно быть пустых строк. 
```
Таким образом я показываю, что эти правила относятся сразу ко всем моим письмам клиентам, а не к какому-либо одному конкретному письму.
## Сокращённый синтаксис
`A ᗉ S` ≡ `A ≔ ᗉ S`.
### Rationale
Используя подобный сокращённый синтаксис, я одним оператором `ᗉ` делаю сразу 2 вещи: делаю утверждение `ᗉ S` и ввожу краткое обозначение `A` для этого утверждения.

# 13. ⦿
## Syntax
~~~code
⦿ S
~~~
## Meaning
```text
Конкретный элемент `S`
```
При этом `S` — это `𝕊` / `𝒞` / `ℂ` / `𝕋`.
## Rationale
###
Я использую такой синтаксис, чтобы делать утверждения об одном конкретном (не произвольном) элементе `S`. 
`S` — это `𝕊` / `𝒞` / `ℂ` / `𝕋`.
###
Семантически в естественном английском языке это соответствует использованию определённого артикля «the» совместно с термином вроде «element»: «the element».
## Какой элемент я имею в виду?
Сам по себе синтаксис `⦿ S` не содержит конкретики, какой именно элемент я имею в виду.
Эту конкретику я выражаю отдельно одним из 2 способов:
### Способ 1
В дальнейшей части моего запроса / документа.
#### Example
~~~code
`ꆜ` ≔ ⦿ ⠿ ⟨ потенциальный клиент на Upwork ⟩
Информация о `ꆜ`: <…>
~~~
### Способ 2
Используя мета-данные: смотри §4 выше.
#### Example
~~~code
`I⠿` ≔ ⠿ ⟨ приложенный к этому запросу файл ⟩
`I₁` ≔ ⟪ filename: `1.png` ⟫ ⦿ `I⠿` 
~~~
## Сокращённый синтаксис
`A ⦿ S` ≡ `A ≔ ⦿ S`.
### Rationale
Используя подобный сокращённый синтаксис, я одним оператором `⦿` делаю сразу 2 вещи: делаю утверждение `⦿ S` и ввожу краткое обозначение `A` для этого утверждения.
### Examples
####
~~~code
`ꆜ` ⦿ ⠿ ⟨ потенциальный клиент на Upwork ⟩
Информация о `ꆜ`: <…>
~~~
####
~~~code
`I⠿` ≔ ⠿ ⟨ приложенный к этому запросу файл ⟩
`I₁` ⟪ filename: `1.png` ⟫ ⦿ `I⠿` 
~~~
###
По смыслу `⦿` соответствует определённому артиклю «the» английского языка.

# 14. ᛭DLᵢ
`᛭DLᵢ` ᗉ `᛭DL⠿`

# 15. ᛭Dc
`᛭Dc` — это обозначение `᛭DLᵢ` самого себя.
Другими словами, если текст `᛭DLᵢ` содержит упоминание `᛭Dс` — это значит, что `᛭DLᵢ` упоминает сам себя. 
## Example
Если имя файла `᛭Dᵢ` — `sample.md`, и текст `sample.md` использует обозначение `᛭Dc`, это значит, что `᛭Dc` в данном случае обозначает документ `sample.md`.    

# 16. ∷
## Syntax
~~~code
A∷B
~~~
## Meaning 
Информация `B` из (`A` ⦿ `᛭D⠿`).
## Example
A∷⟨ пункт 3 ⟩

# 17. ☷
`☷` ᗉ ⠿ ⟨ фрагмент `᛭Dᵢ` ⟩

# 18. §
## Syntax
~~~code
§P
~~~
## Meaning
`☷`, состоящий из пункта `P`.
Другими словами, пункт `P` из `᛭Dᵢ`. 
## Example
`A∷§3` ≡ `A∷⟨ пункт 3 ⟩`
## Сокращённый синтаксис
`§` без `∷` означает `᛭Dc∷§`.

# 19. §{P₁, P₂, …, Pₙ}
## Meaning
`☷` из пунктов `P₁, P₂, …, Pₙ`. 
## Examples
###
`§{2, 3, 5}` означает «Пункты `⠿{2, 3, 5}` из `᛭Dc`».
###
`A∷§{2, 3, 5}` означает «Пункты `⠿{2, 3, 5}` из `A`».

# 20. §┫A - B┣
## Meaning
⟪ пункты ᛭Dᵢ ⟫ ┫A - B┣ 
Другими словами: `☷` от §A до §B, включая §A и §B. 
## Examples
###
`§┫2 - 5┣` означает `⟪ пункты ᛭Dc ⟫ ┫2 - 5┣`.
###
`С∷§┫2 - 5┣` означает `⟪ пункты C ⟫ ┫2 - 5┣`

# 21. §┫- B┣
## Meaning
`☷` от начала и до пункта `B` включительно.

# 22. §┫A -┣
## Meaning
`☷` от пункта `A` включительно и до конца `᛭Dᵢ`
  
# 23. ⌠…⌡
## Syntax
~~~code
A⌠☷⌡ ≔ B
~~~
## Meaning
###
Для понятия `B` я временно, только в рамках `☷`, использую обозначение `A`.
###
Вне `☷` это обозначение не действительно: в частности, если до `☷` обозначение `A` имело другой смысл, то после `☷` обозначение `A` снова будет иметь этот смысл.
###
По сути, `A` — это локальная переменная с областью действия `☷`.
При такой аналогии, `A ≔ B` объявляет глобальную переменную `A`.
## Example
`A⌠§┫2 - 5┣⌡ ≔ B` означает, что для понятия `B` я временно, только в рамках `§┫2 - 5┣`, использую обозначение `A`. 
## Краткий синтаксис
Символ `§` можно пропускать, например:
`A⌠┫2 - 5┣⌡ ≔ B` ≡ `A⌠§┫2 - 5┣⌡ ≔ B`

# 24. `≔†`
~~~code
A ≔† B
~~~
means:
```code
(`A` ≔ `B`) AND (`B` is a **problem** to me).
```

# 25. `▶`
```code
▶ A
```
означает, что в описываемой мной ситуации я использую `A`.

# 26. `⧙ ⧘`
## Syntax
```code
A⧙P₁, P₂, …, Pₙ⧘ ≔ ⟨ D1 ⟩ ⸨ 
	D2
⸩
```
## Meaning
###
`A` — сущность, значение которой зависит от параметров `P₁, P₂, …, Pₙ`.
###
`D1` — определение сути `A`.
###
`D2` — определение параметров `A`.
`D2` —  опциональный блок.
Вместо него параметры могут быть определены в `D1`.  

# 27. `⧛ ⧚`
## Syntax
```code
A⧛V₁, V₂, …, Vₙ⧚
```
## Meaning
Такой синтаксис используется в связке с синтаксисом §26.
Он означает сущность `A` при конкретных значениях параметров `P₁, P₂, …, Pₙ`, равных `V₁, V₂, …, Vₙ`

# 28. `߷`
##
`߷⠿` ≔ ⠿~ ⟨ приложенные к этому запросу файлы ⟩

##
`߷ᵢ` ᗉ `߷⠿`

##
### Syntax
```code
߷⧙F⧘
```
### Meaning
⟪ имя файла: `F` ⟫ ⦿ `߷⠿`
Другими словами: `߷ᵢ` с именем файла `F`.
### Example
߷⧛1⧚

# 18. `≔⊥`
~~~code
A ≔⊥ (B, C)
~~~
обозначает, что я вижу противоречие между `B` и `C` и обозначаю это противоречие как `A`.
Альтернативная запись:
```code
A ≔ (B, C ⊢ ⊥)
```

# 19. `⌖`
### Syntax
#### Variant 1
```
⌖ ⟦A⟧ ❮ T ❯
```
#### Variant 2
~~~code
⌖ ⟦A⟧ 
```
T
```
~~~
### Meaning
`T` is a citation from `A`.

# 20. `ꘖ` (attributes / properties)
## 20.1. Definitions using a global symbol
### 20.1.1. 
#### Syntax
~~~code
ꘖ A ∋ B
~~~
#### Meaning
`B` is an attribute / property of `A`.

### 20.1.2.
#### Syntax
~~~code
ꘖ A ∋ B ≔ ⟪ D ⟫ 
```
V
```
~~~
#### Meaning
~~~code
(ꘖ A ∋ B) AND (B ≔ ⟪ D ⟫ V)
~~~

## 20.2. Definitions using local keys
### 20.2.1. Common rules
####
В §20.1 мы описывали an attribute / property of `A` using глобально доступный затем символ `B`.
####
В §20.2 мы описываем attributes / properties of `A` иначе: using local keys.
####
Эти local keys имеют уникальное значение только в контексте `A`.
####
Вне контекста `A` эти local keys могут иметь другие значения.
Поэтому при сссылке на эти local keys надо обязательно указывать их : `A`.
Конкретный синтаксис для указания контекста описан в §20.2.4. 

### 20.2.2.
#### Syntax
##### Variant 1
~~~code
ꘖ A ∋ ⟨ B ⟩
~~~
##### Variant 2
~~~code
ꘖ A ∋ ⟨ B ⟩ ⟪ D ⟫
~~~

#### Meaning
~~~code
	(`B` is an attribute / property of `A`) 
AND 
	(`B` is a local key, not a standalone entity)
AND 
	(`D` — это комментарий, поясняющий роль `B` в `A`)	
~~~

### 20.2.3.
#### Syntax
~~~code
ꘖ A ∋
```toml
'B1' = 'V1'
'B2' = 'V2'
`B3` = 'V3'
<…>
``` 
~~~
#### Meaning
~~~code
ꘖ A ∋ ⟨ B1 ⟩ ≔ V1
ꘖ A ∋ ⟨ B2 ⟩ ≔ V2
ꘖ A ∋ ⟨ B3 ⟩ ≔ V3
<…>
~~~

### 20.2.4.
#### Syntax
~~~code
A「B」
~~~
#### Meaning
#####
Таким способом мы ссылаемся на local key `B`, определённый ранее в качестве attribute / property of `A` посредством синтаксиса §20.2.2 или §20.2.3.  
#####
Требования к `A「B」` распространяются и все подразделы внутри `A「B」` (например, `A「B」「С」`).
