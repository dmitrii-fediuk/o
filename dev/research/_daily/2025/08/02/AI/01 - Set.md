## 1. `≔`
### 1.1.
```code
A ≔ B 
```
means that `A` **denotes** `B`.

### 1.2.
~~~code
A ≔
```
B
```
~~~
равнозначно `A ≔ B` и используется, когда `B` — многострочный текст.

## 2. `→`
~~~code
A → B
~~~
denotes a material conditional (https://en.wikipedia.org/wiki/Material_conditional)

## 3. `⊢`
~~~code
A ⊢ B
~~~
denotes a logical consequence (https://en.wikipedia.org/wiki/Logical_consequence)

## 4. `⊤`
### 4.1.
~~~code
⊤ B
~~~
means that `B` is true (is a fact).

### 4.2.
~~~code
⊤⟦Rs⟧ B
~~~
means:
```
(⊤ `B`) AND (`Rs` are the reasons why `B` is true)
```

### 4.3.
~~~code
A ≔⊤
```
B
```
~~~
means:
```code
(`A` ≔ `B`) AND (⊤ `B`).
```

### 4.4.
~~~code
A ≔⊤⟦Rs⟧
```
B
```
~~~
means:
```code
(`A` ≔ `B`) AND (⊤⟦Rs⟧ B).
```

## 5. `≔!`
### 5.1.
~~~code
A ≔! B
~~~
means:
```code
(`A` ≔⊤ `B`) AND (`B` is surprising).
```

### 5.2.
~~~code
A ≔!⟦Rs⟧ B
~~~
means:
```code
(`A` ≔⊤⟦Rs⟧ `B`) AND (`B` is surprising).
```

## 6. `≔?`
### 6.1.
~~~code
A ≔? B
~~~
means:
```code
(`A` ≔ `B`) AND (`B` is a hypothesis).
```

### 6.2.
~~~code
A ≔?⟦Rs⟧ B
~~~
means:
```code
(A ≔? B) AND (`Rs` are the reasons for the hypothesis).
```

## 7. `§`
~~~code
§P
~~~
means:
```
the point `P` of the current prompt
```
  
## 8. Local Definitions
### 8.1.
~~~code
A[§P] ≔ B
~~~
means:
```markdown
- Для понятия `B` я **временно**, **только в рамках** §`P`, использую обозначение `A`.
- Вне §`P` это правило не применяется: в частности, если до §`P` обозначение `A` имело другой смысл, то после §`P` обозначение `A` снова будет иметь этот смысл.
- По сути, `A[§P] ≔ B` объявляет **локальную переменную** `A` с **областью действия** §`P`.
- В отличие от `A[§P] ≔ B`, `A ≔ B` объявляет **глобальную переменную** `A`.
```

### 8.2.
~~~code
A[§P₁, §P₂, …, §Pₙ] ≔ B
~~~
имеет значение, **аналогичное §8.1** , только в этом случае обозначение `A` имеет значение `B` в контексте **не одного пункта** §`P`, а **всех** пунктов [§`P₁`, §`P₂`, …, §`Pₙ`], **перечисленных** в квадратных скобках.

### 8.3.
~~~code
A[§P₁-§Pₙ] ≔ B
~~~
имеет значение, **аналогичное §8.2**, только в этом случае я в квадратных скобках не перечисляю все пункты §`P₁`, §`P₂`, …, §`Pₙ`, а вместо этого указываю их **диапазон**.
Обозначение `A` имеет значение `B` в контексте **всех** пунктов этого диапазона, **включая** его **границы**: §`P₁` и §`Pₙ`.


## 9. `≔⠿`
### 9.1.
~~~code
S ≔⠿ {I₁, I₂, …, Iₙ}
~~~
будет обозначать, что `S` — множество, заданное точным перечислением всех его элементов: {`I₁`, `I₂`, …, `Iₙ`}.

### 9.2.
~~~code
S ≔⠿ {I₁-Iₙ}
~~~
означает, что `S` — множество, заданное интервалом (диапазоном) его значений.
`S`, в том числе, включает границы указанного интервала: `I₁` и `Iₙ`.

### 9.3.
~~~code
S ≔⠿ (D)
~~~
означает, что `S` — это множество, и `D` — это неформальное (словесное) определение/описание элементов `S`.

### 9.4.
~~~code
S ≔⠿
```
D
```	
~~~
равнозначно `S ≔⠿ (D)` и используется, когда `D` — многострочный текст.

### 9.5.
~~~code
S ≔⠿ (D)
```yaml
- I₁
- I₂
- …
- Iₙ
```	
~~~
означает: (`S ≔⠿ (D)`) AND ({`I₁`, `I₂`, …, `Iₙ`} — примеры некоторых (не всех) элементов `S`) .

## 10.
### 10.1.
~~~code
A : S ≔ B
~~~
means:
```code
(`A` ≔ `B`) AND (`A` ∈ `S`).
```

### 10.2.
~~~code
A : S
~~~
means:
```code
`A` : `S` ≔ (an arbitrary element of `S`)
```

## 11. `≔†`
~~~code
A ≔† B
~~~
means:
```code
(`A` ≔ `B`) AND (`B` is a **problem** to me).
```

## 12. `▶`
```
▶ A
```
означает, что в описываемой мной ситуации я использую `A`.

## 13. 
`Rs` ≔⠿ {§1-§12}

## 14.
### 14.1.
`AIs` ≔⠿
```text
Современные AI, основанные на LLM и GPT.
В частности: Gemini 2.5 Pro.
```

### 14.2.
`PT` ≔ (A prompt: некий запрос пользователя к `AIs`)

### 14.3.
`T` ≔ (Задача формулирования `PT`)

### 14.4.
`L` ≔ (Некий полуформальный или формальный язык для `T`)

### 14.5.
`Ls` ≔⠿ (множество всех `L`)

### 14.6.
`L0` : `Ls` ≔ (`L`, состоящий из правил `Rs`)

## 15. 
`As` ≔⠿ (Авторитетные источники информации)
```yaml
- arxiv.org
- научные труды по математике, логике, информатике
- GitHub
- Reddit
- сайты StackExchange
- другие авторитетные источники на английском языке.
```

## 16.
`Upwork` ≔ (https://en.wikipedia.org/wiki/Upwork)

## 17.
### 17.1.
`PT-16` ≔ (правило §16)

### 17.2.
`S-1.1` ≔ (синтаксис §1.1)

### 18.3.
⊤ (`PT-16` — пример применения `S-1.1` )

## 19.
### 19.1.
`BT3` ≔ (Символы «3 backticks» и «3 tildas», которые я заимствовал из Markdown и использую в `Rs`)

### 19.2.
`MFs` ≔⠿ (Markdown Flavors: потенциальное указание языка для `BT3`)
```yaml
- `text`
- `markdown`
- `yaml`
```

### 19.3.
`MFi` : `MFs`

## 20.
### 20.1.
`PT-19.2` ≔ (правило §19.2)

### 20.2.
`S-9.5` ≔ (синтаксис §9.5)

### 20.3.
⊤ (`PT-29.2` — пример применения `S-9.5`)

## 21.
### 21.1.
`Q` ≔ (Вопрос «сколько элементов в `MFs`»)

### 21.2.
`P1` ≔† (`AIs` часто путаются при ответе на `Q` и дают неверный ответ «3»)

## 22.
### 22.1.
`S1s` ≔⠿ {§9.1, §9.2}

### 22.2.
`S2s` ≔⠿ {§9.3-§9.5}

### 22.3.
`Ss` ≔⠿ (`S1s` ⋃ `S2s`)

### 22.4.
`S1i` : `S1s`

### 22.5.
`S2i` : `S2s`

### 22.6.
`Si` : `Ss`

### 23.
### 23.1.
`H1` ≔? (`S1s` описывает `Si` более формально, чем `S2s` )

### 23.2.
`H2` ≔?
```
Возможно, причиной `P1` является то, что  `AIs` путают степень формальности `S1i` и `S2i`
```

### 23.3.
`H3` ≔?
```
Возможно, `P1` можно устранить, используя разный синтаксис для  `S1i` и `S2i`
```

### 23.4.
`H4` ≔?
```
Для реализации `H3`, возможно, имеет смысл заменить `⠿` на другой символ либо для `S1i`, либо для `S2i`
```

### 23.5.
`H5` ≔?
```
Для реализации `H3`, возможно, имеет смысл заменить добавить к либо для `S1i`, либо для `S2i` какой-либо новый символ
```

## 24.
### 24.1.
`H`[§24] ≔ `H4`

### 24.2.
`Ps`[§24-§26] ≔⠿ (доводы за `H`)

### 24.3.
`Pi`[§24-§26] : `Ps`

### 24.4.
`Cs`[§24-§26] ≔⠿ (доводы против `H`)

### 24.5.
`Ci`[§24-§26] : `Cs`

### 24.6.
`As`[§24-§26] ≔⠿ (`Ps` ⋃ `Cs`)

 ### 24.7.
`Ai`[§24-§26] : `As`

### 24.8.
`AiS`[§24-§26] ≔ («Score» для `Ai`: целое число от 0 до 100, отражающее значимость `Ai`)

### 24.9.
`V`[§24-§26] ≔⠿ (твоё итоговое, обоснованное, объективное мнение о `H`)

### 24.10.
`HS`[§24-§26] ≔ («Score» для `H`: целое число от 0 до 100, отражающее твою оценку `H`)

### 24.11.
`R`[§24-§26] ≔ (итоговый отчёт)

## 25. Твоя задача
Действуй последовательно, по следующим шагам:
25.1. Определи и систематизируй `Cs`.
25.2. Определи и систематизируй `Ps`.
25.3. Совместно проанализируй `Cs` и `Ps`. 
Для каждого `Ai` определи `AiS`. 
25.4. На основе этого анализа составь `V` и определи `HS`.
25.5. Предоставь `R` в соответствии с требованиями §26

## 26. Требования к формату `R`
### 26.1. 
`X` ≔
```xml
<?xml version='1.0' encoding='UTF-8' ?>
<analysis>
	<pros>
		<argument score='`SCORE`'><![CDATA[`ARGUMENT`]]></argument>
		<argument score='`SCORE`'><![CDATA[`ARGUMENT`]]></argument>
	</pros>
	<cons>
		<argument score='`SCORE`'><![CDATA[`ARGUMENT`]]></argument>
		<argument score='`SCORE`'><![CDATA[`ARGUMENT`]]></argument>
	</cons>
	<verdict score='`SCORE`'><![CDATA[`VERDICT`]]></verdict>
</analysis>
```

### 26.2. 
`R` должен быть документом XML структуры `X`.

### 26.3.
`R` должен использовать для отступов табуляцию, не пробелы.

### 26.4.
`R` должен использовать для параметров тегов XML одинарные кавычки, не двойные.

### 26.5.
Описание параметров `X`:
- `SCORE` внутри `<argument>`: `AiS` 
- `SCORE` внутри `<verdict>`: `HS` 
- `ARGUMENT`: `Ai`
- `VERDICT`: `V`

## 27. Дополнительные требования
1) Уже известную мне информацию не пересказывай.
2) В своём анализе используй авторитетные источники информации на английском языке.
3) Свой ответ дай на русском языке. 
4) Обязательно используй свой режим «Deep Research».
Твой ответ без режима «Deep Research» — гарантированно неверный.
