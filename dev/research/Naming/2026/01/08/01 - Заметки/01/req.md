# 
У меня на компьютере имеются 2 файла (`FF`) в формате Markdown.
#
`FF` у меня постоянно открыты: я ими постоянно пользуюсь при работе за компьютером.
# 
Назначение `FF`: я копирую

#
Первый из `FF` (`F1`) содержит множество (больше 1000) символов Unicode. 
Я пользуюсь `F1` в 2 сценариях:
##
Когда хочу вставить в какой-либо из своих текстов символ, отсутствующий на клавиатуре.
##
Когда хочу найти редкий символ для своих целей.
Моя первичная цель при этом — разработка формального языка (`L`) для построения онтологий.
`L` мне нужен для общения с современными LLM.

#
Второй из `FF` (`F2`) содержит множество простых (коротких) шаблонов (часто использущих `L`) для запросов к LLM

# `G↑`
Я хочу поместить `FF` в общую для них папку (`D`).
В `D` будут только `FF`.

# `᛭T`
Придумай 40 вариантов `A` имени для `D`.
Каждый вариант обоснуй.

#
`A` должны быть на английском языке.

#
`A` должно быть одним словом.

# Варианты `A`, которые пока мне нравятся больше всего
Их не повторяй.
##
- `assets`
- `lexicon`
##
Возможно, стоит подумать над словами типа «everyday».

# Варианты `A`, которые мне не нравятся
Их не повторяй.
```
## 2. Glyphs

Название делает акцент на визуальной составляющей файла `F1`, который наполнен множеством редких графических знаков Unicode.
Термин профессионально описывает элементы начертания, что добавляет технической строгости вашей библиотеке символов.

## 3. Codex

Слово вызывает ассоциации с древним рукописным сводом, что придаёт фундаментальный вес вашей коллекции знаний.
Такое имя отлично подходит для упорядоченного хранения правил онтологии и базовых шаблонов языка `L`.

## 4. Syntax

Файлы в папке `D` задают синтаксические конструкции, необходимые для корректного построения запросов к LLM.
Имя указывает на то, что здесь хранятся строительные блоки, определяющие структуру вашего формального взаимодействия.

## 5. Semantics

Ваша работа над онтологиями нацелена на передачу точного смысла, что делает это название максимально релевантным.
Символы из `F1` и шаблоны из `F2` выступают здесь как чистые носители семантических значений для модели.

## 6. Protocol

Папка содержит утверждённый вами протокол обмена данными и строгие правила коммуникации с искусственным интеллектом.
Это технически точное название для набора стандартов, регулирующих ввод информации через язык `L`.

## 7. Interface

Ваши файлы `FF` служат прослойкой и пультом управления между вашим мышлением и алгоритмами языковой модели.
Папка буквально является интерфейсом, через который вы транслируете свои намерения в понятный машине код.

## 8. Matrix

Структурированный набор из тысяч символов и шаблонов образует цифровую матрицу для генерации новых смыслов.
Название отсылает к технологической среде и системному подходу в разработке ваших онтологий.

## 9. Forge

Папка `D` — это место, где вы создаёте и закаляете инструменты для эффективной интеллектуальной работы.
Метафора кузницы отлично описывает процесс конструирования сложных запросов из базовых элементов `F1` и `F2`.

## 10. Inputs

Это утилитарное имя честно описывает роль файлов как источника входных данных для внешних систем.
Оно удобно своей краткостью и отсутствием двусмысленности в описании направления потока информации.

## 11. Context

Материалы папки задают необходимый контекст, без которого модель не сможет правильно интерпретировать ваши команды.
Именно содержимое `FF` определяет рамки и условия для корректной работы нейросети с вашим языком.

## 12. Source

Для всех ваших текстов и онтологий эта папка является первоисточником и базой исходного кода.
Название подчёркивает, что именно отсюда вы берёте кирпичики для построения любых сложных структур.

## 13. Keys

Символы и шаблоны служат ключами, открывающими доступ к специфическим возможностям и режимам работы LLM.
Каждый элемент из `F1` подбирается для достижения конкретного результата, подобно уникальному ключу.

## 14. Runes

Редкие символы Unicode визуально напоминают магические руны, что придаёт эстетику вашей работе с кодом.
Имя добавляет творческий оттенок процессу составления формальных промптов на языке `L`.

## 15. Signals

Файлы формируют четкие управляющие сигналы, которые вы посылаете машине для получения желаемой реакции.
Это название подходит для кибернетического взгляда на коммуникацию через формальный язык.

## 16. Toolkit

Папка представляет собой ваш персональный набор инструментов для решения задач онтологического проектирования.
Оно акцентирует внимание на прикладном характере использования файлов для постоянного копирования.

## 17. Basis

Коллекция `FF` формирует фундамент для всех ваших дальнейших построений и экспериментов с текстом.
Без этого базиса невозможна стабильная и продуктивная работа с формальными онтологиями.

## 19. Nexus

Слово означает точку связи, символизируя место встречи человеческих идей и машинной логики.
Папка выступает центральным узлом, объединяющим все элементы вашей системы взаимодействия с LLM.

## 20. Core

Файлы `FF` являются ядром вашей деятельности, так как они постоянно открыты и используются в работе.
Название кратко и емко передает суть незаменимости этих инструментов для вашего рабочего процесса.
```


#
По умолчанию отвечай на русском языке.

#
Не пиши в своём ответе самовольно введение / вступление.
Мне это пизобольство не нужно.
Вместо этого сразу отвечай конкретно, по существу `᛭T`.
##
В частности, не начинай свой ответ с бесполезных фраз типа «Хороший вопрос» и другого подобного мусора.

#
Не пиши в своём ответе самовольно заключение.
Мне это пизобольство не нужно.
Просто в точности выполни `᛭T`.
Экономь моё время: не мусори словами.
##
В частности, в конце своего ответа не задавай мне мусорные вопросы типа «Хотите я <…>?».
То, что я хочу — конкретно указано в `᛭T`.
Фокусируйся на `᛭T` и не навязывай мне другие свои услуги.

#
##
Каждый абзац твоего ответа должен содержать ровно одно предложение.
##
Mежду абзацами твоего ответа не должно оставаться пустых строк.

#
Никогда не используй `ꑌ𐓩J`.
Используй только `ꑌ𐓩O`.

# URL
Все URL пиши в виде plain text, без разметки Markdown.
Другими словами: тебе запрещено обрамлять URL посредством `[]` и `()`.  

# Маркированные списки
В качестве маркера маркированных списков всегда используй `ꑌ¶-`, не `ꑌ¶*`. 

# Кавычки / `ꑌ¶G`
##
Используй `ꑌ¶G` в качестве кавычек по умолчанию.
##
Также обрамляй посредством `ꑌ¶G`:
###
Названия элементов User Interface.

# Кавычки / `ꑌ¶B`
Обрамляй посредством `ꑌ¶B`:
##
Точные `ꑌ𐏕`.
##
Имена компьютерных файлов, команд, программ и т.п..
Например: `header.php`.

#
Не меняй форматирование `᛭A`, если тебе явно не сказано это делать.

#
В разметке Markdown твоего ответа не используй `\` перед `.`, `[`, `]` и другими подобными символами.
## Пример
### Неправильное форматирование
```markdown
# 1\. Заголовок
```
### Правильное форматирование
```markdown
# 1. Заголовок
```

# Markdown / Заголовки
## Структура твоего ответа
### 
У твоего ответа не должно быть одного общего заголовка 1-го уровня (`#`), потому что твой ответ будет вставлен внутрь секции 1-го уровня (`#`) другого документа Markdown.
###
Исходя из предыдущего пункта, в качестве заголовков верхего уровня ты должен использовать заголовки 2-го уровня (`##`).
Таких заголовков должно быть несколько: тем самым ты разбиваешь свой ответ на разделы.
Если твой ответ краток, то не разбивай его на разделы вообще.
### 
Разумеется, ты также можешь использовать заголовки более нижних уровней внутри заголовков 2-го уровня: для дополнительной структуризации текста.

## 
Никогда не используй выделение жирным (`**`) в заголовках.

## 
Всегда форматируй заголовки только символами решётки (`#`), не другими способами. 

## 
Не вставляй самовольно полностью пустые строки после заголовков Markdown.
### Пример
#### Неправильное форматирование
```markdown
# Заголовок

Текст
```
#### Правильное форматирование
```markdown
# Заголовок
Текст
```

## Нумерация заголовков
### 
По умолчанию делай заголовки нумерованными.
###
Не меняй формат нумерации в `᛭A`, если тебе явно не сказано это делать.
#### 
В частности, если в `᛭A` используется нумерация `1)`, `2)`, `3)`, то не меняй самовольно `)` на `.` 

# Режим «Deep Research» / Общее
## 
Не пиши никаких эссе в режиме «Deep Research».
Просто выполни `᛭T`: больше ничего не надо.
Не пиши никаких «введений» / «вступлений» и «заключений».
##
Пиши кратко: я использую режим «Deep Research» для сбора точных фактов в Интернете, а не для чтений пустословия.
## 
Не создавай самовольно никаких иллюстраций.
Мне нужен только текст.

# Режим «Deep Research» / Требования к источникам информации / При анализе юридических вопросов
##
В первую очередь используй официальные нормативные акты тех стран, закондательства которых касается `᛭T`.

##
Поиск информации веди на официальных языках тех стран, закондательства которых касается `᛭T`.

##
В своём ответе сошлись на конкретные пункты конкретных нормативных актов, с конкретными цитатами из них.
Цитаты давай как в оригинальном варианте (как они записаны в нормативном акте), так и в своём переводе.

# Режим «Deep Research» / Требования к источникам информации / Общее
## 
Если `᛭T` не касается специфики не-англоговорящей страны, то в первую очередь используй источники информации на английском языке.
##
В первую очередь используй официальные источники информации.
###
В частности, для программного обеспечения в первую очередь используй его официальную документацию.
##
Большое внимание уделяй анализу опыта реальных пользователей.
Для этого в первую очередь используй social media platforms where users can express their opinions independently of the platform owner:
- Reddit
- GitHub
- Сайты сети StackExchange
- Официальные форумы
- Неофициальные форумы
- Авторитетные сайты отзывов типа Trustpilot, sikayetvar.com и т.п.

# Правила ответа / Для текстов на английском языке
## 
Не используй сокращения типа «don't». Все подобные фразы пиши полностью: «do not».

## 
Никогда не переводи понятие «сайт» / «веб-сайт» как «site». 
Вместо этого используй форму «website»: это является более профессиональным.

## 
###
Никогда не переводи понятие «пункт нумерованного списка» как «item».
### 
Для пунктов нормативных актов вместо «item» используй тот термин, который принято использовать в данном юридическом контексте: «paragraph», «section» и т.п.
### 
Для всех остальных текстов переводи «item» как «point».

##
Вместо «for example» в тексте на английском языке используй «e.g.».
При этом не забывай, что в начале предложения эта фраза должна начинатся с заглавной буквы: «E.g.»

# Правки программого кода
## 
Никогда не пиши в коде свои комментарии.

##
Никогда не убирай из кода уже имеющиеся там комментарии.

##
Никогда не пиши в коде полностью пустые строки.

##
Отступы в коде делай табуляцией, не пробелами.

##
Строковые константы всегда по возможности обрамляй одинарными кавычами (') вместо двойных (").

##
Если я даю тебе программный код для внесения в него правок, то мой код никогда полностью не переписывай. 
Вместо этого предлагай конкретные точечные пронумерованные по пунктам правки.
Каждая правка должна быть оформлена как заголовок первого уровня (`#`).
 
 