## 1. `≔`
### 1.1.
```code
A ≔ B 
```
means that `A` **denotes** `B`.

### 1.2.
~~~code
A ≔
```
B
```
~~~
равнозначно `A ≔ B` и используется, когда `B` — многострочный текст.

## 2. `≔†`
~~~code
A ≔†
```
B
```
~~~
means:
```code
(`A` ≔ `B`) AND (`B` is a **problem** to me).
```

## 3. `≔§`
### 3.1.
~~~code
A[P] ≔§
```
B
```
~~~
означает, что для понятия `B` я **временно**, **только в рамках пункта** `P`, использую обозначение `A`.
Вне пункта `P` это правило не применяется: в частности, если до пункта `P` обозначение `A` имело другой смысл, то после пункта `P` обозначение `A` снова будет иметь этот смысл.
По сути, `≔§` объявляет **локальную переменную** `A` с **областью действия** `P`.
В отличие от `≔§`, `≔` объявляет **глобальную переменную** `A`.

### 3.2.
~~~code
A[P₁, P₂, …, Pₙ] ≔§
```
B
~~~
имеет значение, **аналогичное** пункту **3.1**, только в этом случае обозначение `A` имеет значение `B` в контексте **не одного пункта** `P`, а **всех** пунктов [`P₁`, `P₂`, …, `Pₙ`], **перечисленных** в квадратных скобках.

### 3.3.
~~~code
A[P₁-Pₙ] ≔§
```
B
```
~~~
имеет значение, **аналогичное** пункту **3.2**, только в этом случае я в квадратных скобках не перечисляю все пункты `P₁`, `P₂`, …, `Pₙ`, а вместо этого указываю их **диапазон**.
Обозначение `A` имеет значение `B` в контексте **всех** пунктов этого диапазона, **включая** его **границы**: `P₁` и `Pₙ`.

## 4. `⊤`
### 4.1.
~~~code
⊤ B
~~~
means that `B` is true (is a fact).

### 4.2.
~~~code
⊤
```
B
```
~~~
равнозначно `⊤ B` и используется, когда `B` — многострочный текст.

### 4.3.
~~~code
A ≔⊤
```
B
```
~~~
means:
```code
(`A` ≔ `B`) AND (⊤ `B`).
```

## 5. `≔!`
~~~code
A ≔!
```
B
```
~~~
means:
```code
(`A` ≔⊤ `B`) AND (`B` is surprising).
```

## 6. `≔?`
~~~code
A ≔?
```
B
```
~~~
means:
```code
(`A` ≔ `B`) AND (`B` is a hypothesis).
```

## 7. `≔⠿`
### 7.1.
~~~code
S ≔⠿ {I₁, I₂, …, Iₙ}
~~~
будет обозначать, что `S` — множество, заданное точным перечислением всех его элементов: {`I₁`, `I₂`, …, `Iₙ`}.

### 7.2.
~~~code
S ≔⠿ (D)
~~~
означает, что `S` — это множество, и `D` — это неформальное (словесное) определение/описание элементов `S`.

### 7.3.
~~~code
S ≔⠿
```
D
```	
~~~
равнозначно `S ≔⠿ (D)` и используется, когда `D` — многострочный текст.

### 7.4.
~~~code
S ≔⠿ (D)
```yaml
- I₁
- I₂
- …
- Iₙ
```	
~~~
означает: (`S ≔⠿ (D)`) AND ({`I₁`, `I₂`, …, `Iₙ`} — примеры некоторых (не всех) элементов `S`) .

## 8.
### 8.1.
~~~code
A : S ≔
```
B
```
~~~
means:
```code
(`A` ≔ `B`) AND (`A` ∈ `S`).
```

### 8.2.
~~~code
A : S
~~~
means:
```code
`A` : `S` ≔ (an arbitrary element of `S`)
```

## 9. `▶`
```
▶ A
```
означает, что в описываемой мной ситуации я использую `A`.

## 10. 
`Rs` ≔⠿ (Правила пунктов 1-9 выше)

## 11.
`AIs` ≔⠿
```text
Современные AI, основанные на LLM и GPT.
В частности: Gemini 2.5 Pro.
```

## 12.
`PT` ≔ (A prompt: некий запрос пользователя к `AIs`)

## 13.
`T` ≔ (Задача формулирования `PT`)

## 14.
`L` ≔ (Некий полуформальный или формальный язык для `T`)

## 15.
`Ls` ≔⠿ (множество всех `L`)

## 16.
`L0` : `Ls` ≔ (`L`, состоящий из правил `Rs`)

## 17. 
`As` ≔⠿ (Авторитетные источники информации)
```yaml
- arxiv.org
- научные труды по математике, логике, информатике
- GitHub
- Reddit
- сайты StackExchange
- другие авторитетные источники на английском языке.
```

## 18.
`Upwork` ≔ (https://en.wikipedia.org/wiki/Upwork)

## 19.
### 19.1.
`PT-18` ≔ (правило пункта 18, выше)

### 19.2.
`S-1.1` ≔ (синтаксис пункта 1.1, выше)

### 19.3.
⊤
```
`PT-18` — пример применения `S-1.1` 
```

## 20.
### 20.1.
`PT-19.1` ≔ (правило пункта 19.1, выше)

### 20.2.
`PT-19.2` ≔ (правило пункта 19.2, выше)

### 20.3.
`PTs` ≔⠿ {`PT-19.1`, `PT-19.2`}

### 20.4.
`PTi` : `PTs` 

### 20.5.
`RFs` ≔⠿ (Ссылки в `PTi` на другие пункты)
```yaml
- «пункта 19.1, выше» в `PT-19.1`
- «пункта 19.2, выше» в `PT-19.2`
```

### 20.6.
`RFi` : `RFs` 

## 21.
### 21.1.
`K` ≔
```markdown
Текущий неформальный (словесный) синтаксис `RFi`.
```

## 21.2.
`KAs` ≔⠿ (Альтернативы `K`)

## 21.3.
`KAi` : `KAs` ≔ (Произвольный элемент `KAs`)


## 22. Требования и пожелания к `KAi`
1) `KAi` должен быть лаконичным: по аналогии с синтаксисом `Rs`.
2) `KAi` должен быть стилистически единобразен с синтаксисом правил `Rs`.
3) Возможно, стоит использовать что-либо типа символа параграфа (`§`).
Однако, конкретно именно этот символ уже используется в пункте 3, и не хотелось бы, чтобы `AIs` путались.

## 23. Твоя задача
1) Предложи мне 20 `KAs` в виде таблицы из 2 колонок:
1.1) `KAi` (выражение, которое должно включать 1 параметр: тот пункт, на который мы ссылаемся).
Это выражение записывай так, чтобы используемые тобой символы Unicode нормальным образом отображались в твоём ответе: не пиши хуйню с `\`. 
1.2) Твоё обоснование.
2) До и после таблицы ничего не пиши: в ответе должна быть только таблица.  
3) На остальные вопросы не отвечай.
4) Уже известную мне информацию не пересказывай.
5) Обязательно используй свой режим «Deep Research».
Твой ответ без режима «Deep Research» — гарантированно неверный.
6) В своём анализе используй `As`.
7) Свой ответ дай на русском языке. 


