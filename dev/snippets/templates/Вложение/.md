# Вложение

##
Во вложении — текущий DOM страницы STUB
Он получен посредством инструмента «Select an element in the page to inspect it» в DevTools в Google Chrome 139 в режиме Incognito.
Для краткости из него удалены теги `style` и `svg` с их содержимым.

##
Во вложении — исходный код той же страницы STUB
Он получен посредством пункта «View page source» контекстного меню Google Chrome.

##
Во вложении — содержимое консоли Google Chrome 139 после загрузки страницы STUB

##
Во вложении — результат выполнения `JSON.stringify(window.google_tag_data);` в консоли браузера на странице `STUB` в контексте эксперимента §§2.7-2.8 сразу после завершения этого эксперимента.

##
Во вложении — URLs всех запросов, которые делает браузер для отображения страницы STUB
Этот список получен посредством инструмента «Copy» → «Copy All URLs» в DevTools в Google Chrome.

##
Во вложении — HAR для той же страницы STUB.
Этот список получен посредством инструмента «Copy» → «Copy All as HAR (sanitized)» в DevTools в Google Chrome 139.
Установлен фильтр: `facebook.com`.

##
Во вложении — HAR для той же страницы STUB.
Он получен посредством инструмента «Export HAR» в DevTools в Google Chrome 139.

#
Какую новую важную для проекта информацию ты получил?
#
Для каждого утверждения указывай:
- доводы за
- доводы против
- твою оценку в %
#
Все утверждения доказывай цитатами.
# Цитаты программного кода
##
Используй backticks.
##
Для цитат длиной выше 50 символов используй блочный синтаксис.
##
В заголовке блока указывай тип контента (по правилам Markdown).
##
Identation блочных цитат начинай с нуля (слева).
##
В URL внутри цитат не пихай от себя Markdown.

##
Цитаты давай сначала на языке оригинала, а затем — в своём переводе.
##
Для каждого утверждения указывай также дату и имя.


#
Во вложении — снова STUB.
Какая ещё ранее неупомянутая, но важна для проекта информация там есть?
##
Все утверждения доказывай цитатами.
Цитаты давай сначала на языке оригинала, а затем — в своём переводе.
##
Для каждого утверждения указывай дату и имя.

#
Во вложении — Network Activity в формате HAR.
Какую новую важную для проекта информацию ты получил?

##
Во вложении — видео процесса прокрутки страницы STUB