# 16. ᛭DLᵢ
`᛭DLᵢ` ᗉ `᛭DL⠿`

# 17. ᛭Dc
`᛭Dc` — это обозначение `᛭DLᵢ` самого себя.  
Другими словами, если текст `᛭DLᵢ` содержит упоминание `᛭Dс` — это значит, что `᛭DLᵢ` упоминает сам себя. 
## Example
Если имя файла `᛭Dᵢ` — `sample.md`, и текст `sample.md` использует обозначение `᛭Dc`, это значит, что `᛭Dc` в данном случае обозначает документ `sample.md`.    

# 19. ☷⧙D⧘
`☷⧙D⧘` ≔ ⟨ ᗉ ⠿~ ⟨ фрагмент `D`⟩ ⟩ ⸨ `D`: a document ⸩

# 20. ᛭A☷
`᛭A☷` ≔ `☷⧛᛭A⧚`

# 21. §
## Syntax
~~~code
§P
~~~
## Meaning
`☷⧙᛭Dᵢ⧘`, состоящий из пункта `P`.  
Другими словами, пункт `P` из `᛭Dᵢ`. 
## Example
`A∷§3` ≡ `A∷⟨ пункт 3 ⟩`
## Сокращённый синтаксис
`§` без `∷` означает `᛭Dc∷§`.

# 22. §{P₁, P₂, …, Pₙ}
## Meaning
`☷⧙᛭Dᵢ⧘` из пунктов `P₁, P₂, …, Pₙ`. 
## Examples
###
`§{2, 3, 5}` означает «`☷⧛᛭Dc⧚` из пунктов `⠿{2, 3, 5}`».
###
`A∷§{2, 3, 5}` означает «`☷⧛A⧚` из пунктов `⠿{2, 3, 5}`».

# 23. §┫A - B┣
## Meaning
⟪ пункты ᛭Dᵢ ⟫ ┫A - B┣   
Другими словами: `☷⧙᛭Dᵢ⧘` от §A до §B, включая §A и §B. 
## Examples
###
`§┫2 - 5┣` означает `⟪ ☷⧛᛭Dc⧚ ⟫ ┫2 - 5┣`.
###
`С∷§┫2 - 5┣` означает `⟪ ☷⧛С⧚ ⟫ ┫2 - 5┣`

# 24. §┫- B┣
## Meaning
`☷⧙᛭Dᵢ⧘` от начала и до пункта `B` включительно.

# 25. §┫A -┣
## Meaning
`☷⧙᛭Dᵢ⧘` от пункта `A` включительно и до конца `᛭Dᵢ`

# 26. §┫-┣
## Meaning
`᛭Dᵢ` целиком.
  
# 27. ⌠…⌡
## Syntax
~~~code
⌠S⌡ ⟨ rules ⟩
~~~

## Meaning
Правила `<rules>` действуют временно: только в рамках `S`.  
`S` — это ⟨ Ⱳ Scope_(computer_programming) ⟩ для `<rules>`.  
Вне `S` правила `<rules>` не действительны. 

## Главный Use Case: введение local variables
### Rationale
⟨ Ⱳ Local_variable ⟩ сокращают мне запись: я могу в рамках `☷` вводить короткие обозначения, не беспокоясь о конфликте этих обозначений с остальной частью моего запроса.

### Syntax
~~~code
⌠S⌡ ⟨ 
A₁ ≔ B₁ 
A₂ ≔ B₂ 
⟩
~~~

### Meaning
`A₁`, `A₂` — это local variables.   
Вне `S` они могут иметь другие значения вместо `B₁` и `B₂`.

### Example
`⌠§┫2 - 5┣⌡ A ≔ B`

## Краткий синтаксис
Символ `§` можно пропускать, например:
`⌠┫2 - 5┣⌡ A ≔ B` ≡ `⌠§┫2 - 5┣⌡ A ≔ B`

# 29. ▶
## Syntax
```code
▶ A
```
## Meaning
Это означает, что в описываемой мной ситуации я использую `A`.
## Example
```
▶ Windows 10
```

# 30. ߷⠿
`߷⠿` ≔ ⠿~ ⟨ приложенные к этому запросу файлы ⟩

# 31. ߷ᵢ
`߷ᵢ` ᗉ `߷⠿`

# 32. ߷⧙F⧘
### Syntax
```code
߷⧙F⧘
```
### Meaning
⟪ имя файла: `F` ⟫ ⦿ `߷⠿`
Другими словами: `߷ᵢ` с именем файла `F`.
### Example
߷⧛1⧚

# 33. ⊥
## Syntax
~~~code
⊥ (A, B)
~~~
## Meaning
Противоречие между `A` и `B`.
Другими словами: `(A, B ⊢ ⊥)`.

# 34. ⌖
### Syntax
#### Variant 1
```
⌖ ⟦A⟧ ❮ T ❯
```
#### Variant 2
~~~code
⌖ ⟦A⟧ 
```
T
```
~~~
### Meaning
`T` is a citation from `A`.

# 35. ꘖ
## 35.1. Definitions using a global symbol
### 45.1.1. 
#### Syntax
~~~code
ꘖ A ∋ B
~~~
#### Meaning
`B` is an attribute / property of `A`.

### 35.1.2.
#### Syntax
~~~code
ꘖ A ∋ B ≔ ⟪ D ⟫ 
```
V
```
~~~
#### Meaning
~~~code
(ꘖ A ∋ B) AND (B ≔ ⟪ D ⟫ V)
~~~

## 35.2. Definitions using local keys
### 35.2.1. Common rules
####
В §35.1 мы описывали an attribute / property of `A` using глобально доступный затем символ `B`.
####
В §35.2 мы описываем attributes / properties of `A` иначе: using local keys.
####
Эти local keys имеют уникальное значение только в контексте `A`.
####
Вне контекста `A` эти local keys могут иметь другие значения.
Поэтому при сссылке на эти local keys надо обязательно указывать их : `A`.
Конкретный синтаксис для указания контекста описан в §35.2.4. 

### 35.2.2.
#### Syntax
##### Variant 1
~~~code
ꘖ A ∋ ⟨ B ⟩
~~~
##### Variant 2
~~~code
ꘖ A ∋ ⟨ B ⟩ ⟪ D ⟫
~~~

#### Meaning
~~~code
	(`B` is an attribute / property of `A`) 
AND 
	(`B` is a local key, not a standalone entity)
AND 
	(`D` — это комментарий, поясняющий роль `B` в `A`)	
~~~

### 35.2.3.
#### Syntax
~~~code
ꘖ A ∋
```toml
'B1' = 'V1'
'B2' = 'V2'
`B3` = 'V3'
<…>
``` 
~~~
#### Meaning
~~~code
ꘖ A ∋ ⟨ B1 ⟩ ≔ V1
ꘖ A ∋ ⟨ B2 ⟩ ≔ V2
ꘖ A ∋ ⟨ B3 ⟩ ≔ V3
<…>
~~~

### 35.2.4.
#### Syntax
~~~code
A「B」
~~~
#### Meaning
#####
Таким способом мы ссылаемся на local key `B`, определённый ранее в качестве attribute / property of `A` посредством синтаксиса §35.2.2 или §35.2.3.  
#####
Требования к `A「B」` распространяются и все подразделы внутри `A「B」` (например, `A「B」「С」`).


# 6. ⧙ … ⧘
## Syntax
```code
A⧙P₁, P₂, …, Pₙ⧘ ≔ ⟨ D1 ⟩ ⸨ 
	D2
⸩
```
## Meaning
###
`A` — сущность, значение которой зависит от параметров `P₁, P₂, …, Pₙ`.
###
`D1` — определение сути `A`.
###
`D2` — определение параметров `A`.
`D2` —  опциональный блок.
Вместо него параметры могут быть определены в `D1`.  

# 7. ⧛ … ⧚
## Syntax
```code
A⧛V₁, V₂, …, Vₙ⧚
```
## Meaning
Такой синтаксис используется в связке с синтаксисом §6.
Он означает сущность `A` при конкретных значениях параметров `P₁, P₂, …, Pₙ`, равных `V₁, V₂, …, Vₙ`

# ꕤ⧙T⧘
`ꕤ⧙T⧘` ≔ ⟨ the `ꑌꕤ` of `T` ⟩ ⸨ `T` is a `ꑌ𐏕`⸩

# ꕤ?⠿⧙T⧘
##
`ꕤ?⠿⧙T⧘` ≔ ⠿ ⟨ `ꑌꖿ` о потенциальных `ꑌꕤ` of `T` ⟩ ⸨ `T` is a `ꑌ𐏕`⸩

##
`ꖿ` `ꕤꖿ⠿⧙T⧘`

##
`ꕤꖿᵢ⧙T⧘` ᗉ `ꕤꖿ⠿⧙T⧘`

##
`ꖿ` `ꕤꖿᵢ⧙T⧘`
