## 1. `≔`
### 1.1.
```code
A ≔ B 
```
means that `A` **denotes** `B`.

### 1.2.
~~~code
A ≔
```
B
```
~~~
равнозначно `A ≔ B` и используется, когда `B` — многострочный текст.

## 2. `→`
~~~code
A → B
~~~
denotes a material conditional (https://en.wikipedia.org/wiki/Material_conditional)

## 3. `⊢`
~~~code
A ⊢ B
~~~
denotes a logical consequence (https://en.wikipedia.org/wiki/Logical_consequence)

## 4. `⊤`
### 4.1.
~~~code
⊤ B
~~~
means that `B` is true (is a fact).

### 4.2.
~~~code
⊤⟦Rs⟧ B
~~~
means:
```
(⊤ `B`) AND (`Rs` are the reasons why `B` is true)
```

### 4.3.
~~~code
A ≔⊤
```
B
```
~~~
means:
```code
(`A` ≔ `B`) AND (⊤ `B`).
```

### 4.4.
~~~code
A ≔⊤⟦Rs⟧
```
B
```
~~~
means:
```code
(`A` ≔ `B`) AND (⊤⟦Rs⟧ B).
```

## 5. `≔!`
### 5.1.
~~~code
A ≔! B
~~~
means:
```code
(`A` ≔⊤ `B`) AND (`B` is surprising).
```

### 5.2.
~~~code
A ≔!⟦Rs⟧ B
~~~
means:
```code
(`A` ≔⊤⟦Rs⟧ `B`) AND (`B` is surprising).
```

## 6. `≔?`
### 6.1.
~~~code
A ≔? B
~~~
means:
```code
(`A` ≔ `B`) AND (`B` is a hypothesis).
```

### 6.2.
~~~code
A ≔?⟦Rs⟧ B
~~~
means:
```code
(A ≔? B) AND (`Rs` are the reasons for the hypothesis).
```

## 7. `§`
~~~code
§P
~~~
means:
```
the point `P` of the current prompt
```
  
## 8. Local Definitions
### 8.1.
~~~code
A[§P] ≔ B
~~~
means:
```markdown
- Для понятия `B` я **временно**, **только в рамках** §`P`, использую обозначение `A`.
- Вне §`P` это правило не применяется: в частности, если до §`P` обозначение `A` имело другой смысл, то после §`P` обозначение `A` снова будет иметь этот смысл.
- По сути, `A[§P] ≔ B` объявляет **локальную переменную** `A` с **областью действия** §`P`.
- В отличие от `A[§P] ≔ B`, `A ≔ B` объявляет **глобальную переменную** `A`.
```

### 8.2.
~~~code
A[§P₁, §P₂, …, §Pₙ] ≔ B
~~~
имеет значение, **аналогичное §8.1** , только в этом случае обозначение `A` имеет значение `B` в контексте **не одного пункта** §`P`, а **всех** пунктов [§`P₁`, §`P₂`, …, §`Pₙ`], **перечисленных** в квадратных скобках.

### 8.3.
~~~code
A[§P₁-§Pₙ] ≔ B
~~~
имеет значение, **аналогичное §8.2**, только в этом случае я в квадратных скобках не перечисляю все пункты §`P₁`, §`P₂`, …, §`Pₙ`, а вместо этого указываю их **диапазон**.
Обозначение `A` имеет значение `B` в контексте **всех** пунктов этого диапазона, **включая** его **границы**: §`P₁` и §`Pₙ`.


## 9. `≔⠿`
### 9.1.
~~~code
S ≔⠿ {I₁, I₂, …, Iₙ}
~~~
будет обозначать, что `S` — множество, заданное точным перечислением всех его элементов: {`I₁`, `I₂`, …, `Iₙ`}.

### 9.2.
~~~code
S ≔⠿ {I₁-Iₙ}
~~~
означает, что `S` — множество, заданное интервалом (диапазоном) его значений.
`S`, в том числе, включает границы указанного интервала: `I₁` и `Iₙ`.

### 9.3.
~~~code
S ≔⠿ (D)
~~~
означает, что `S` — это множество, и `D` — это неформальное (словесное) определение/описание элементов `S`.

### 9.4.
~~~code
S ≔⠿
```
D
```	
~~~
равнозначно `S ≔⠿ (D)` и используется, когда `D` — многострочный текст.

### 9.5.
~~~code
S ≔⠿ (D)
```yaml
- I₁
- I₂
- …
- Iₙ
```	
~~~
означает: (`S ≔⠿ (D)`) AND ({`I₁`, `I₂`, …, `Iₙ`} — примеры некоторых (не всех) элементов `S`) .

## 10.
### 10.1.
~~~code
A : S ≔ B
~~~
means:
```code
(`A` ≔ `B`) AND (`A` ∈ `S`).
```

### 10.2.
~~~code
A : S
~~~
means:
```code
`A` : `S` ≔ (an arbitrary element of `S`)
```

## 11. `≔†`
~~~code
A ≔† B
~~~
means:
```code
(`A` ≔ `B`) AND (`B` is a **problem** to me).
```

## 12. `▶`
```
▶ A
```
означает, что в описываемой мной ситуации я использую `A`.

## 14. 
`X` ≔
```xml
<?xml version='1.0' encoding='UTF-8' ?>
<analysis>
	<pros>
		<argument score='`SCORE`'><![CDATA[`ARGUMENT`]]></argument>
		<argument score='`SCORE`'><![CDATA[`ARGUMENT`]]></argument>
	</pros>
	<cons>
		<argument score='`SCORE`'><![CDATA[`ARGUMENT`]]></argument>
		<argument score='`SCORE`'><![CDATA[`ARGUMENT`]]></argument>
	</cons>
	<итоговое_заключение score='`SCORE`'><![CDATA[`итоговое заключение`]]></итоговое_заключение>
</analysis>
```

## 15. 
`T` ≔ (Тег `<итоговое_заключение>` внутри `X`)

## 16. Твоя задача
1) Мне нужно перевести `T` на английский язык.
2) Перевод должен быть валидным именем тега в XML.
3) Предложи мне 20 вариантов такого перевода.
4) Результат должен быть таблицей из 20 строк и 2 колонок: 
4.1) первая колонка — для `T` 
4.2) вторая — твоё обоснование перевода
5) До и после таблицы ничего не пиши.

