# ᛭A⧙…⧘
## 
`᛭A⧙P₁, P₂, …, Pₙ⧘` ≔ ⠿ ⟨ Пункты `᛭A` с номерами `P₁, P₂, …, Pₙ`  ⟩

## 
`᛭A⧙P₁-Pₙ⧘` ≔ ⠿ ⟨ Пункты `᛭A` с номерами от `P₁` до `Pₙ` включительно ⟩
```
Если в качестве `P₁` указан `0` — это означает «начало `᛭A`»
```

## 
`᛭A⧙D⧘` ≔ ⟨ Фрагмент `᛭A` ⟩
```
`D` либо указывает конкретный пункт `᛭A`,  либо словесно описывает границы фрагмента. 
```

# `𐒌⠿`
## 
`𐒌⠿` ≔ ⠿~ ⟨ Замечания к `᛭A` ⟩ 

##
`𐒌ᵢ` : `𐒌⠿`

##
ꘖ `𐒌ᵢ` ∋ ⟨ `Points` ⟩ ⟪ Перечень пунктов `᛭A`, к которым относится `𐒌ᵢ` ⟫

##
ꘖ `𐒌ᵢ` ∋ ⟨ `Citations` ⟩ 
```
Конкретные цитаты из `᛭A`, к которым относится `𐒌ᵢ`.
Эти цитаты должны быть законченными предложениями. 
```

##
ꘖ `𐒌ᵢ` ∋ ⟨ `Essence` ⟩ ⟪ Твой текст `𐒌ᵢ` ⟫

##
ꘖ `𐒌ᵢ` ∋ ⟨ `Confidence` ⟩ 
```
Твоя степень уверенности в истинноcти твоего `𐒌ᵢ` по шкале от 0 до 100:
- 0 означает, что `𐒌ᵢ` безосновательно (ошибочно).
- 100 означает, что ты полностью уверен в правоте своего `𐒌ᵢ`.
```

## `𐒌ᵢ「Ontology」`
###
ꘖ `𐒌ᵢ` ∋ ⟨ `Ontology` ⟩ ⟪ Раздел `𐒌ᵢ`, предназначененный для улучшения `᛭OE` ⟫

###
ꘖ `𐒌ᵢ「Ontology」` ∋ ⟨ `Contradictions` ⟩ 
```
Перечень утверждений `᛭OE` (с указанием их пунктов), которые противоречат `𐒌ᵢ`.
Получается, что ты с вероятностью `𐒌ᵢ「Confidence」` считаешь эти утверждения ошибочными.
Здесь запрещено упоминать `᛭A`.
Здесь запрещено упоминать вводимые в `᛭A` обозначения.
```

###
ꘖ `𐒌ᵢ「Ontology」` ∋ ⟨ `NewRules` ⟩ 
```
Новые знания (сформулированные в виде правил), которые надо добавить к `᛭E`, чтобы впредь не повторять `𐒌ᵢ`.
Здесь запрещено упоминать `᛭A`.
Здесь запрещено упоминать вводимые в `᛭A` обозначения.
```

###
ꘖ `𐒌ᵢ「Ontology」` ∋ ⟨ `Corrections` ⟩ 
```
#
Конкретные правки, которые надо внести в `᛭OE` для устранения `𐒌ᵢ「Ontology」「Contradictions」`.
#
Для каждой правки должен быть указан конкретный уже существующий пункт `᛭OE`.
#
Здесь запрещено упоминать `᛭A` и тем более указывать правки к `᛭A`.
#
Здесь запрещено упоминать вводимые в `᛭A` обозначения.
```

#
`𐒌O` ≔ `𐒌ᵢ「Ontology」`, включая все его подразделы

# Требования к `𐒌ᵢ「Ontology」` (и всем его подразделам)
##
`𐒌ᵢ「Ontology」` не предназначен для улучшения `᛭A`.
##
В `𐒌ᵢ「Ontology」` запрещено упоминать `᛭A`.
##
`𐒌ᵢ「Ontology」` не должен содержать правки к `᛭A`.
##
`𐒌ᵢ「Ontology」` не должен использовать контекст `᛭A` (и тем более вводимые в `᛭A` обозначения).
`𐒌ᵢ「Ontology」` должен быть понятен вне контекста `᛭A`.
##
`𐒌ᵢ「Ontology」` может и должен использовать синтаксис `᛭L`.
##
`𐒌ᵢ「Ontology」` может и должен использовать `᛭OE` и вводимые там обозначения.

# Требования к `𐒌ᵢ「Ontology」「NewRules」`
##
Для новых правил используй идентификаторы в формате `Eⵂ<номер>`, где `<номер>` — целочисленный порядковый номер обозначения со сквозной нумерацией внутри `᛭E`.

#
`𐒌ᵢ「Ontology」「NewRules」` ⋃ `𐒌ᵢ「Ontology」「Corrections」` не может быть пустым множеством: иначе`᛭E` не получит новых знаний для избежания повторений `𐒌ᵢ` в будущем.

#
##
`𐒌⠿⧙P₁, P₂, …, Pₙ⧘` ≔ ⠿ ⟨ `𐒌ᵢ` из `𐒌⠿` с номерами `P₁, P₂, …, Pₙ`  ⟩

##
`𐒌⠿⧙P₁-Pₙ⧘` ≔ ⠿ ⟨ `𐒌ᵢ` из `𐒌⠿` с номерами от `P₁` до `Pₙ` включительно ⟩
