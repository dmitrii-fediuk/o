# 1.
##
`A¦⠿` ≔ ⠿~ ⟨ Важные аспекты, которые надо учесть при `T⁎` ⟩

## 
`A¦ᵢ` ᗉ `A¦⠿`

##
ꖿ `A¦ᵢ`

## 
`Iⰳ(A¦ᵢ)` ≔ ⟨ Важность `A¦ᵢ` ⟩

# 2. `᛭T`
Действуй пошагово
## 
Найди `A¦⠿`.
## 
Проанализируй `A¦⠿`.
Для этого для каждого  `A¦ᵢ` выяви:
### 
Доводы за `Iⰳ(A¦ᵢ)`.
###
Доводы против `Iⰳ(A¦ᵢ)`.
## 
Оцени `Iⰳ(A¦ᵢ)` по шкале от 0 до 100:
- 0 означает, что ты полностью уверен, что `A¦ᵢ` неважен.
- 100 означает, что ты полностью уверен, что `A¦ᵢ` важен.
##
Выскажи свой вердикт.

# 5. Требования к процессу анализа
##
Не решай задачи, не относящиеся к `᛭T`.
##
Обязательно используй свой режим «Deep Research».
Твой ответ без режима «Deep Research» — гарантированно неверный.
##
Очень осторожно относись в своём анализе к мнению `ꆜ`: оно может быть ошибочно. 

# 6. Требования к ответу / Форматирование
##
Каждый `A¦ᵢ` оформляй посредством Markdown как раздел (`A¦ᵢ-S`) 2-го уровня (`##`).
## 
Внутри `A¦ᵢ-S` должны присутствовать следующие подразделы, оформленные посредством Markdown как разделы 3-го уровня (`###`):
- Суть
- Оценка
- Доводы за
- Доводы против
##
###
Каждый абзац `A¦ᵢ` должен содержать ровно одно предложение.
###
Между абзацами `A¦ᵢ` не должно оставаться пустых строк.
