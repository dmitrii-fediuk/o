#
Отлично.
После твоей правки код стал такой:
~~~python
STUB
~~~

# Задача
Теперь предложи правки, чтобы во всех функциях переменная, возвращающая результат функции, называлась `r` (а не `ret_path` и т.п.).
